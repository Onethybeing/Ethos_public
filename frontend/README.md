# Frontend

React 19 + Vite. Proxies API calls to `localhost:8000`.

## Start

```bash
npm install
npm run dev     # → http://localhost:5173
npm run build   # production build → dist/
npm run lint
```
